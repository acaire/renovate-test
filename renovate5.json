{
  extends: ["config:best-practices"],
  includePaths: [".nvmrc", "Dockerfile"],
  rebaseWhen: "behind-base-branch",
  packageRules: [
    {
      groupName: "NodeJS",
      groupSlug: "nodejs-dependencies",
      matchPackageNames: ["node"],
      minimumReleaseAge: "3 days",
    }
  ]
}
